# Apply Manifest
**Revision 323** — supersedes Revision 322 and earlier. **`bin/restore-repos.sh` gains `--repo`, the twin of `--stage`.** `--stage` narrows a run to one kind of work; `--repo` narrows it to one repository, named by label, so `--hydrate --stage ignored-files --repo my-service` puts one repository's kept ignored files back without walking every other clone on the way. **It narrows what is acted on, not what is reported.** `restore-status.md` stays an inventory of everything the pre-image audit recorded, because narrowing a run should not make the rest of the machine disappear; only `hydrated.md` narrows, and a repository outside the scope gets no row at all rather than a row claiming an outcome for work nobody asked for. **A name the audit does not carry is an error**, checked before the plan even loads and answered with the labels it does carry — an unvalidated filter matches nothing and produces a run that hydrates nothing, reports no error, and reads exactly like a run that found nothing to do. **Scope rides in the `Mode` column of `repo-restore-index.md`** rather than in a column of its own: that index is append-only and its header is written once, so a tenth column would break every index already sitting on an artifact volume. A scoped run records `hydrate (repo: my-service)` there, and prints a `Repositories:` line in both `hydrated.md` and the run summary. `restore-repos.md` carries the option through the workflow overview, the terminology table's `Label` row, Step 1's sample summary, and a third command block in Step 5. Modified: `bin/restore-repos.sh`, `restore-repos.md`.

**Revision 322** — supersedes Revision 321 and earlier. **Written under the owner's section 6 override by a session that had already closed**, and named as one here because §5 says a `closed` session may not write and the only exception is this. **The commission shape is settled and written up.** `docs/architecture/commissions-bays-and-blueprints.md` is new: a commission holds **bays**, a bay holds **concerns** and **specifications** and produces one **blueprint**, and `master-plan.md` indexes them. **It answers the kind Tier 0 said had no name** — the word is `mode`, `brownfield` or `greenfield`, orthogonal to `kind` and enforceable, because a brownfield commission's specifications must name what they displace and a greenfield one has nothing to point at. **A bay is a dossier**, genus `bay`, which makes a commission the first **compound dossier** — one whose members are dossiers — so a commission's standing derives on the same crank a dossier's does rather than on a second copy of it; Revision 308 is the record of what a second copy costs. **Not `layer`**: this tree defines a layer as a behaviour of the running system and already records that the review's first pass got that wrong, so a unit of design work needed its own word. **Three new closed sets, every value unused today** so the disjointness guard needs no declared overlap — `stage` on a concern, `course` on a bay, `adoption` on a specification — and **`determination` is the new class name**, standing to `outcome` and `adoption` as `disposition` stands to `status`, `standing`, `state` and `stage`. **Inputs are concerns, not questions**, on ISO 42010's word, because *we want X, what shape* is a third of what arrives and `questions.md` holds the rest only by mislabelling it. **Concerns and specifications are many-to-many**, so a concern is `designed` only when every specification citing it is terminal, not when one is. **`course` derives from concerns, not specifications**: a bay could have every specification adopted while one concern sits uncited, and deriving from specifications reads that bay as drawn over a concern nobody answered. **The blueprint check sits outside the derivation**, because inside it a missing blueprint makes the bay silently not-drawn — `0037` F5's shape. **Current-versus-future is a pointer, not two documents**: the baseline is a commit recorded per bay when that bay enters drafting, future state is the union of adopted specifications, and every specification names what it displaces. **And a ruling is made against a wording that moves** — a citation carries a digest of the concern as worded, because `answersAsOf` already exists for exactly this at **39 of 152 populated** and nothing reads it. `docs/ideas/big-picture.md` gains the register and the pointers; the architecture document is authoritative. **Nothing here is built** and no checker is proposed — `0043` D3's ruling stands, fixture set first. Baselines unmoved: `counts` FAIL 0, `headers` FAIL 11, `structure` OK 110 FAIL 0, completeness 0, doc-paths 0/0, `stamp --dry-run` writes nothing.

**Revision 321** — supersedes Revision 320 and earlier. **The session that designed the run index closes, three dossiers short of deriving it.** `run-index-design-20260901-000000` had stood `handoff` since 2026-09-02 without ever satisfying the precondition — no successor existed and nothing was transferred — and the JSON retrofit carried that declaration forward unexamined. **`closed` did not derive**: eight of its eleven dossiers were terminal, six `answered` and `0009`/`0026` `superseded`, and three were `assigned` with a single `un-started` member each. `0014`, `0021` and `0034` were released to `unclaimed` under §10a, which is the fourth release in six revisions, and the close was then written: `declaredState` and `state` `closed`, `ended.on` 2026-09-13, the second owner stint closed, `scratchPath` recorded for the first time, `ownedBundles` down to eight, three disposals in the Revision 320 shape, and `final-summary.md`. `stamp` reports 0 records both before and after — the derivation agreed with the declaration at every step. **§10a's "there is no step for setting the status" is true of derivation and false of the data.** Removing the manifest row released the bundles and `check` immediately reported all three as `ORPHAN` — *ownership is null and no manifest lists it* — because each bundle's own `metadata.json` still read `standing: assigned`, `ownership: null`. `0001`, released at Revision 306, carries `unclaimed` in both, which is what the sentence omits. Setting them returned `check` to its exact baseline: 23 findings, `ORPHAN` back to `0002` and `0004` alone. **`0034` is the one worth reading.** This session recorded it on 2026-09-04 and deliberately left it unowned because `restore-git.md` is the owner's active work; a retrofit since then added a manifest row, and ownership derives from the row, so it was owned without anyone deciding to own it — while its index row still named no session. The release ends that disagreement rather than creating one. **The summary records what the session got wrong**: `0030` D7's closing claim that every member carried a decision, false and caught by another session; a hazard described as silent that a measurement showed to be loud; a reading of `restore-git-entry`'s FAILs that was backwards and became `0034`; its own D2, reversed by the owner and kept in place rather than edited away; and a zero-byte file written into an artifact root the session had been told was read-only. **The Bash 3.2 debt is recorded as unreachable rather than outstanding** — both stints ran on Linux with Bash 5.x, and the shell reached through the desktop bridge is a Linux VM, not the Mac host, so `/bin/bash -n` under stock 3.2 cannot be run from this side at all. Baselines unchanged: `headers` 11 FAIL and `doc-currency` 21 review items, identical to the live tree before this revision.

**Revision 320** — supersedes Revision 319 and earlier. **The instruments session closes, and the close is what Revision 317 left unwritten.** Revision 317 released both dossiers and stopped there: `declaredState` stayed null, the session derived `available`, `owners[].until` was null, the `ended` block was empty and no disposals were recorded. **The release was correct and the close was missing**, and §14 names exactly what a `closed` declaration owes — `final-summary.md`, `ended.on`, `owners[].until` — with a precondition that every owned dossier be terminal **or released**, which held from Revision 317 onward. All of it is written here: `declaredState` `closed`, `ended.on` 2026-09-13, the owner stint closed at 2026-09-13, **disposals recorded for both bundles** in the shape Revisions 316 and 319 used — `released`, with the standing at release and the count of members left open — and `final-summary.md`. `stamp` derived `closed` from the declaration. **Six of nineteen members answered.** `0041` leaves six open and `0050` eight, and **none of the fourteen is blocked on measurement**: each is measured and waiting on a decision, which is the one thing a released bundle can carry usefully into the queue. **The summary records what this session got wrong as well as what it built**, because a summary that lists only what worked is not a record: the `0041` F7 disposal, argued by two sessions and reversed at Revision 312 on the owner's test; the `0049` F6 classification, right in verdict and wrong in stated reason; two manifest gaps reported to the owner as permanent when both halves were wrong; a resolution field written under the wrong key twice, which every instrument passed and which is left unfixed because the resolution object is being re-architected; and four failed sweeps whose errors all ran in the direction of making the tree look worse. **Revision 289 has a manifest entry and no commit of its own.** Its content landed inside `15069fe`, whose `Claude-Session` trailer names another session, so the `ended` block lists **thirteen revisions and twelve commits** and the two are left unreconciled rather than padded to match — that difference is `0050` F7 recorded against this session's own work. Baselines unmoved against `ffcfe08`: `counts` OK 81 FAIL 0, `headers` FAIL 11, `structure` OK 113 FAIL 0, completeness 0, suite 77, `stamp --dry-run` writes nothing, doc-paths 0/0, and `check` holds at 23 with its breakdown identical — `ORPHAN` 2 and `UNCITED-DECISION` 21.

**Revision 319** — supersedes Revision 318 and earlier. **`pre-image-capture-conformance-20260903-194532` closes, and the two bundles it recorded itself go back to the queue.** Owner-directed, and the **third release in four revisions** — Revision 316 returned four, Revision 317 two, and this follows their shape. The session was created to read four pre-image runbook findings and decide nothing; it answered all four, recorded four more bundles and closes holding six. **`0007`, `0013`, `0019` and `0025` stand `answered` and `0031` and `0032` stand `superseded` by `0040` and `0041`; all six are retained, because a closed session keeps what it finished and a superseded reading is terminal.** **`0030` and `0035` are released to `unclaimed` with no successor named** — `0030` with five members open (`analyzing`) and `0035` with three (`assigned`) — on the instruction that a bundle a session has not answered goes back to the queue. **Neither is unfinished work that was never started.** Both were declared closed on 2026-09-04 and both shipped their toolkit write: `artifact_run_record_rename` and `artifact_run_rename_lineage` are in `.internal/artifact-runs.sh` and exercised. What was never done is the record — the per-finding tables never moved — which is the defect `run-index-design-20260901-000000` found across four of this session's bundles, and `bin/verify-findings-structure.sh`, written by this session for that class, passes on all four and correctly so. **§10a's four steps are not sufficient, and this is the second revision in a row to find it out.** Removing the manifest row, setting the index row to `—` and `` `unclaimed` `` and decrementing the session's counts (8 → 6 bundles, 20 → 12 members) leaves the bundle's own `ownership` at `null`, so `stamp` went on deriving `analyzing` and `assigned` from the members and `structure` raised both rows as disagreeing with their data. §10a says *there is no step for setting the status ... removing the row IS the release and `unclaimed` follows from it*; **it does not, because `ownership` is authored and not derived** — the derivation that made the sentence true went out with `extract-metadata.py` at Revision 299, which `iris/lifecycles.md` § 3.3 already records and Revision 316 already worked around by hand. `ownership` and `ownershipOn`, the sibling Revision 317 added, are set by hand here too. **Three revisions have now performed a step the instruction does not contain; the instruction is what is wrong.** **The release lowered `check` from 26 to 23 and repaired nothing.** `0035` F1–F3 each carry a resolution over an `un-started` member — the rows Revision 315 named in this tree and **deliberately left visible because their owner may clear them** — and the moment the bundle became `unclaimed` all three moved into the bucket for rows nobody may clear, which is not reported. `0030`'s five moved out of the clone-being-re-read bucket into the same one: 8 rows in 3 unclaimed bundles became 16 in 5. **`0047` F1 and F2 operating exactly as designed, and the arithmetic looks like progress.** **The eleven standing `headers` failures leave with them.** All eleven are in these two bundles — `0030` D1–D8 and `0035` D1–D3, every decision row citing no finding — pre-existing at HEAD and not caused by the release, but `verify-session-findings.sh` still describes that baseline as *"six bundles owned by other sessions"*, which it is not and cannot be while both stand `unclaimed`. **`final-summary.md` is written and `ended` is filled: nine revisions across eight commits** — 178, 183, 184, 185, 187, 191, 194, 195, 196; `7b8695c`, `cf251ab`, `6a53bf5`, `132432b`, `5197268`, `f4159da`, `92a4f79`, `3604743` — **derived from the `Claude-Session` trailer and closed against it in both directions**: every listed commit carries the trailer and no commit carrying it is unlisted. Revisions 194 and 195 shipped in one commit, and **the summary's own contribution table had two of them swapped**, corrected here. **`ended.disposals` is filled for all eight bundles**, which is the first record in this tree to use `superseded` as a disposition — the existing 14 rows use only `answered` and `released`, and the two shapes §4 never specified; **this follows the shape `entity-model-and-vocabulary-20260909-053548` wrote at Revision 316** rather than inventing a third. `owners[].until` is 2026-09-13, `contributions` records the one it made (`0033` F7), and `scratchPath` and `environmentNotes` are filled because closing is the last chance to state where the work was done. **One correction to the session's own `findings-manifest.md`.** A paragraph dated 2026-09-06 said three bundles *left* this session for `docs/session-management-findings/` and became `unclaimed`, and named `0026` among them. **All four claims were wrong**: nothing moved, nothing became `unclaimed`, the successors `0040` and `0041` are separate bundles that were never this session's, and `0026` has been owned by `run-index-design-20260901-000000` throughout. The correction is written with the error named rather than silently replacing it. **What is left owed and named in the summary**: three retroactive `rename` rows, one per category, as evidence writes; `0030` D5's citation repair, queued and unperformed, and **re-censused since by `run-index` at eight volume-side citations rather than the one D5 named**; the §4c numbering command that globbed two findings trees when there were three, **held at the owner's word and relayed to nobody**; and Table 3's banner in `docs/ledgers/evidence-conformance.md`, still reading *"No artifact was migrated"* and untrue since Revision 138. **This revision nearly repeated Revision 313's defect and was caught the same way Revision 316 caught it.** Rewriting the two bundle records through `json.dump(..., ensure_ascii=False)` converted **21 `\u2014` escapes to literal em dashes** across `0030` and `0035`, hiding three lines of real change inside a 56-line diff; both files were restored and edited as text instead, and what changes in each is `standing`, `ownership` and the new `ownershipOn`. **Found by comparing escape counts against `HEAD`, not by reading the diff** — the diff looked plausible. **Baselines**: `counts` FAIL 0; `headers` **FAIL 11 unmoved**; `structure` **OK 113 FAIL 0** — down from 115 on this revision's two fewer manifest comparisons; completeness 0; suite **77, all passing**; `stamp --dry-run` writes **0 records**, which is the assertion that the released records agree with their derivation; doc-paths 0 MISSING / 0 ANCHOR BROKEN; runbook structure 213 PASS / 5 WARN / 25 FAIL across 27 documents; portability WARN 0 FAIL 0; coverage MISSING 0, ORPHANED 6, DUPLICATE 1. **Unclaimed bundles 24 → 26, and live members inside them 96 → 101**, which is the release measured from the other side. **`/bin/bash -n` against macOS stock Bash 3.2 was never run by this session and is owed for every revision it wrote**, as it is from Revision 116 onward; everything here ran in a Linux VM on the owner's machine under Bash 5.1 and Python 3.10. **The declaration is the last thing in the revision**, per §5 — everything the session owed is written before it, because a `closed` session may not write.

**Revision 318** — supersedes Revision 317 and earlier. **The session that designed the allocator closes, owning nothing.** `allocation-and-inquiry-design-20260906-233205` ends after nineteen revisions and fifteen commits with **no bundle to dispose of** — the rule against stranding a bundle is satisfied vacuously rather than by work, which is `0045` F1 exactly: it read `available` for its entire life while writing three architecture records, building the allocator, the interviewer and 48 tests, creating two session bundles and recording six findings bundles it handed to others, and it now reads `closed`, the same word a session gets for finishing twenty findings. **The vocabulary cannot tell those apart and this record is the evidence.** `final-summary.md` carries the disposal, what it got wrong — `0049` recurring twice after being recorded — and six owed items, including thirteen bundles owned by sessions that have stopped. One correction to its own `metadata.md`: a sentence reading *"its unapplied `0043` bundle"* described a scratch copy taken while another session had uncommitted work in the checkout, and misled a reader into thinking `0043` had been transferred here. **Unapplied meant not yet committed, not unassigned.** `0043` was never this session's.

**Revision 317** — supersedes Revision 316 and earlier. **Owner-directed: a session stops holding what it has not answered.** `0041` and `0050` are **released to `unclaimed` with no successor named**, on the instruction that a bundle a session has not answered goes back to the queue rather than waiting on a session that has stopped advancing it. **The second release in two revisions** — `entity-model-and-vocabulary-20260909-053548` returned four at Revision 316 on the same instruction, and this follows its shape. **Neither reached `answered`**: `0041` leaves six members open — F2–F5 `decided` with the remedies unbuilt, F8 and F9 `framing` — and `0050` leaves eight, all `framing` and all measured rather than blocked on measurement. §10a's four steps carried out: the rows removed from `findings-manifest.md`, both index rows to `—` and `` `unclaimed` ``, the session's counts decremented, and **the disposal recorded in the manifest rather than a handoff document, because there is no successor to write one for.** `stamp` derived the rest — both bundles `unclaimed` over progress `analyzing`, and the session `available`. **`available` understates and is left standing.** `docs/legend.md` line 566 defines it as *created or cloned, owning no findings bundle yet*, which misdescribes a session that held two bundles for twenty-seven revisions and released them; that is `0045`'s subject and the same understatement `docs/sessions/INDEX.md` already records for `allocation-and-inquiry-design-20260906-233205`. **No state was declared**, and since Revision 314 none could have been: `available` and `active` are derived-only and `session_state()` raises on either, leaving `handoff`, `dissolved` and `closed` — none of which was asked for, and a session that can still write is not terminal. **§10a requires the release to carry its date and the schema had nowhere to put it, so this revision adds one.** `ownershipOn`, a sibling of `ownership`: `null` or an ISO date, **written by the act and never derived**, declared in `docs/architecture/state-as-data.md` §4.3 and populated on both released bundles. **Additive rather than a reshape, and the reshape was the consistent option.** `lineage` is this tree's idiom for a dated transition — `{ "supersededBy": "0041", "on": "…" }` — and giving `ownership` the same object form would match it; **it was rejected as breaking**, because `bundle_standing` returns `ownership` directly, `OWNERSHIP` is a closed set of strings and 24 records carry one, which makes it a rename with no migration plan — `0052` F2's own example. The sibling breaks nothing and **folds into `standing.on` when §6's disposition retrofit runs**, still gated on a migration plan over 358 records. **Populated on the two released here and on none of the other 22 bundles standing `unclaimed`**, which are closed to everyone — the rule that also gated the `0049` write; their dates are recoverable once from the commit that introduced each value and belong to whoever claims them. **The declaration is separable from the release**: it touches `0043`'s subject matter while `0043` itself stands `unclaimed`, and can be dropped without affecting anything else in this revision. **And at the owner's direction, `0049` F6's `findings.md` cell is corrected** from `decided` to `resolved`, matching data that has read `resolved` with a resolution naming D7 and D8 since Revision 258. **This is an owner-authorised write into an `unclaimed` bundle**, which §line 359 otherwise closes to everyone; taken so the next session to own `0049` does not inherit a false display, and recorded as an exception rather than a precedent. **This session's ledger had the verdict right and the reason wrong**: it filed the row UNCLEARABLE because *moving a member to `resolved` is the owner's act and there is no owner*, when the member had already moved at Revision 258 and the real bar was that the bundle is closed to everyone. **With it cleared, §6.1's last measured class is empty: 232 member-status rows, 0 disagreements.** Baselines: `counts` FAIL 0, `headers` FAIL 11 unmoved, `structure` **OK 115 FAIL 0** — the drop from 119 is Revision 316's four released rows and this revision's two, six fewer manifest comparisons — completeness 0, suite 77, doc-paths 0/0, `stamp --dry-run` writes nothing, and `check` holds at 26 across 57 bundles.

**Revision 316** — supersedes Revision 315 and earlier. **`entity-model-and-vocabulary-20260909-053548` closes, and four bundles it never answered go back to the queue.** Owner-directed. **The order is the rule this session wrote into §5 one revision ago**: everything a closing session owes is written *before* the declaration, because a `closed` session may not write — so the releases, the disposal records, the final summary, the `until` and the `ended` block are all in this revision, and the declaration is the last thing in it. **Four released to `unclaimed` with no successor named** — `0039` (31 members, 20 open, `analyzing`), `0043` (14, 11, `analyzing`), `0045` (5, 5, `analyzing`) and `0048` (3, 3, `transferred`) — on the instruction that a bundle this session had not answered goes back to the queue rather than waiting on a session that is closing. **`0037` is retained**: it stands `answered`, closed at Revision 301, and a closed session keeps what it finished. **§10a's four steps carried out for each**, plus the date Revision 313 added: manifest rows removed, index rows to `—` and `` `unclaimed` ``, the session's counts decremented 5 → 1 bundles and 62 → 9 members, and **the disposal recorded in both `findings-manifest.md` and `final-summary.md` rather than a handoff document, because there is no successor to write one for.** **`0048` is the one to read twice.** It arrived by transfer on 2026-09-09 and **`progress` never left `untouched`**, so this session never wrote to it as owner and **the transfer never cleared**; it is released from the state it arrived in, and §10's *first write as owner* is what makes that a fact rather than a judgement. **Ownership is set by hand and the reason is recorded.** §10a says there is no step for setting the status because removing the manifest row *is* the release — but the derivation that made that true was in `extract-metadata.py`, **retired at Revision 299 with nothing replacing it**, which `iris/lifecycles.md` § 3.3 already records. So `ownership` and `standing` are written to `unclaimed` in each record, exactly as the pending release patch does, and `stamp --dry-run` confirms the four agree with what they derive from. **The session declares `closed` rather than only deriving it.** Both are true here — the one retained bundle is terminal, so the derivation returns `closed` on its own — and the declaration is written because the owner directed the close and §5's new table makes `closed` declarable for exactly that case. `state` and `declaredState` therefore agree, which is the uninteresting outcome and the one worth having: **a session that closed by crossing with neither set is what Revision 313 was about.** **`ended` is filled: 15 revisions across 14 commits** — 269, 271, 273, 274, 275, 279, 285, 287, 290, 293, 296, 301, 303, 314, 315, one commit carrying two — **derived from the `Claude-Session` trailer and cross-checked** against Drift's and the instruments session's own revision lists for overlap, and against `0050` F7's record that `15069fe` misattributes a revision. No overlap and no misattribution touching this session. **`owners[].until` is 2026-09-13**, and `metadata.md` gains four release rows with the note that a release has only an outgoing half, which is why §10a step 4 exists at all. **`final-summary.md` records what the session got wrong**, five items, three of them measurement errors of the same kind: **it asserted `.claude/hooks/` contained no enforcement without checking**, in a review whose whole value was that every claim was measured; **it called `iris/vocabulary.md` the documentation hub at in-degree 11** having measured only `iris/`, where the hub is `docs/legend.md` at 16; **it nearly overwrote Drift's correct 11/12 for `SESSION.transcript`** by counting files instead of the field, and checked first; **it converted an escaped em dash to a literal one while rewriting a session `metadata.json`** — Revision 313's own recorded defect, committed in the session that had just read the record of it, caught by comparing escape counts and redone; and **it twice reported verified baselines against a patch that had silently not reached the device**, caught by checksum both times. **The pattern is one thing: a measurement taken over the wrong population and then trusted because it was a measurement** — *the population is never what you think*, from the page this session wrote, applied four times to the session writing it. **Two residues are named and left alone.** `session-management-re-evaluation-20260906-110105` still lists `0043` in `ownedBundles` although its own `findings-manifest.md` records the transfer away and says so in prose — **the manifest is authoritative, so the release holds** — and `drift-and-the-write-boundary-20260909-053548` and `typed-bundles-architecture-20260908-204724` both still list `0048`. All three are `closed` or `handoff` and none is this session's to edit. **What is handed on**: `0039` F6 and F21, live and never decided, F21 now demonstrated a sixth time by the revision that fixed the fifth; `0056`, the only commission in the tree, `unclaimed` and never worked; the disposition triple, designed and unbuilt behind a 358-record migration; and **`docs/ideas/big-picture.md`**, whose five-tier design order this session's last three revisions exist to start. **Numbers, none of them an `OK` total.** Ten files changed, one of them new. `counts` **FAIL 0**; `headers` **11 FAIL** in `0030` and `0035`, unmoved; `structure` **FAIL 0**; completeness **0 problems**; suite **77, all passing**; **`check` 26 → 26**; `stamp --dry-run` writes **0 records**, which is the assertion that the four released records agree with their derivation; doc-paths clean on the default run; portability **WARN 0, FAIL 0**; coverage MISSING 0, ORPHANED 6, DUPLICATE 1. **Unclaimed bundles 18 → 22 and live members inside them 27 → 75**, which is the release measured from the other side. Measured in a Linux VM on the owner's machine under Bash 5.1 and Python 3.10, **not the macOS Bash 3.2 target**.

**Revision 315** — supersedes Revision 314 and earlier. **Four documents gave four answers to what a session may declare, and the one that ran accepted all five.** Revision 313 constrained `declaredState` in code and handed the prose to this session; this revision carries out that handover, plus the three dated transitions and the vocabulary entries owed with it. **One item is gated and the rest are under the owner's override, and the difference is stated rather than blurred.** §9c's addition carries out `0047` D12, `accepted`, whose F13 is `resolved` — the decision names the remedy as *"a numbered procedure for closing a bundle, in the shape §9b already uses: move the members, then write the note"* and explicitly declines to write it, because §9 is not that session's section. **Everything else has no decided member behind it.** `0039` F6 — *state names and state requirements live in different files* — F1 and F21 are all `framing`, so these are **owner-directed writes under §6's override**, which is the same footing Revision 313 stood on and is named here for the same reason. **§9c gains step 4: write the note last.** The index Notes cell, the manifest row and a session's own prose all describe a closure, and none may be written before step 1 has moved the members. **The tree carries three rows of the failure right now**: `0035` F1, F2 and F3 are each `un-started` and each carries a resolution — the note was written and the members never moved — and `check` has reported `RESOLUTION-AHEAD-OF-FINDING` on all three since. **They are a true positive whose owner may clear them**, which is why they stay visible instead of being exempted; `0030` has the same shape and is licensed, because a clone's members are reset while the re-reading is open, so the exemption is part of the rule rather than a hole in it. **§5 gains what a session may declare and what it owes first.** Declarable is `handoff` · `dissolved` · `closed`; derivable is `available` · `active` · `closed`; **`closed` is in both deliberately**, because the owner may close a session with work outstanding and no derivation produces that. Each declaration gets a precondition that must hold **before** it is written, since the declaration is what ends the session's authority to write: a `handoff` needs a named successor that exists and is assignable — one naming nobody is a release, which is §10a — and `closed` needs every dossier terminal or released with nothing owed unwritten. **`dissolved` requires withdrawing every member**, which is the question the table was written to answer: it is the terminal for *shut down, no further work ever*, and a session holding one `resolved` member did work that stands, so it is `closed`. **The order is §9c's rule again** — everything owed is written before the declaration, not after — and **a session that closes by crossing owes the same things and gets no prompt**, which is exactly how one closed at Revision 313 with an empty `ended` block while nothing looked wrong. **§9 gains the supersession date.** `lineage.on` is set on both sides in the same revision as the tag; **measured, it is null on all 14 records in both directions**, so the tree records that a replacement happened and never when. The section also now says a supersession names a successor that exists — a `supersededBy` pointing at a dossier never created leaves the predecessor terminal and the reading nowhere, and *dropped with nothing replacing it* is `withdrawn`, which the section already said and the date makes checkable. **§10 gains the transfer date and fixes its target rule.** The target must be **assignable** — `available` or `active` — not merely `available`, which excluded `active` and so excluded the ordinary case, a session already holding work. **The band is what the rule means**: a session in `handoff`, `closed` or `dissolved` may not be a transfer target, because each says it takes no new work, and `assignable_sessions()` computes the same set so the rule and the allocator cannot disagree. The date is step 5, on the ground that **a transfer leaves the least behind of any transition** — the outgoing manifest row is gone and `ownership` is a bare string. **Three prose sources are reconciled against the code.** `iris/lifecycles.md` § 4 said *five states, one is declared* and drew a diagram with `dissolved` missing from it entirely; `iris/procedure.md` § 8 said *a session that hands off declares it; every other state follows from what it owns*, implying two; `docs/legend.md` phrased **`dissolved` as a derivation** — *every bundle it owns is `withdrawn`* — when **no derivation returns `dissolved` at all**, and phrased `closed` as *`resolved`, `superseded` or `withdrawn`*, which is two member statuses and one dossier standing in a list of dossier standings, the collision Revision 233 separated. All four now say declarable three, derivable two, `closed` both. **`0039` F21 is demonstrated a sixth time by this very revision** — *nothing checks that a vocabulary change reached the prose* — and is left `framing` rather than quietly resolved, because a write carried out under an override does not answer a finding nobody decided. **`docs/legend.md` and `iris/vocabulary.md` gain the disposition vocabulary**, the latter as a new § 0 with its own contents entry, because sections 3, 4 and 6 each give a closed set and nothing named the class they belong to. **disposition** · **transition** · **crossing** · **declaration** · **band**, with a crossing silent and needing detection and a declaration self-announcing and carrying its obligations inline. **Three bands are tabled** — `INERT` of `status`, `TERMINAL` of `standing`, `ASSIGNABLE` of `state` — with **`un-started` explicitly not inert** (`INERT` means *no work remains*, `un-started` means *all of it does*; including it prices six dossiers at zero and drops 31 of 116 open members from the allocator's count) and **`terminal`, never `terminated`**, because the band names a condition a record is in and not something done to it. **A fourth band exists and does not carry the word**: `RE_READING` = `un-started` · `framing`, meaning *no judgement has been recorded for this member yet* — the exact complement of `INERT` — recorded with the proposed name `UNJUDGED` and **not decided**. **Two subsets are recorded as not bands**: `HARD`, because an edge kind is not a disposition, and `DECLARABLE`, which is a subset of `state`'s values but classifies a **write by who may make it** rather than a record by its condition. **And the `live` collision at `iris/lifecycles.md` is fixed**: `live` already means *not inert*, and a code path that executes is a third thing, so a guard is now **reachable** or **unreachable** and `live` stays with members. **`docs/architecture/state-as-data.md` § 6.1 gains the disposition triple.** Every column in the map that renders a disposition renders one field of three — `{value, by, on}` — and **`by` is what makes one field safe to recompute**: `stamp` recomputes where `by` is `crossing` and leaves `declaration` alone, which is what `state` and `declaredState` do today, stated instead of inferred from a null. **`on` is not `updatedAt`**, which answers *when was this record last written* and moves when prose is edited. Recorded as **designed and not built**: § 6's migration gate fires over 358 records and the verifications come before the retrofit. **And the `analyzing` ruling, held since Revision 306, is written.** Both `docs/legend.md` and `iris/lifecycles.md` now state the row **positively as well as residually**: `analyzing` holds exactly when there is a `framing` or `decided` member, or an `un-started` member beside one that is not — **48 combinations by the first clause, 7 by the second, zero counterexamples across all 63 non-empty presence sets.** **Both forms are kept and the reason is the ruling**: the residual is what the code does and the tables render the code in order, while a residual **asserts nothing**, so a derivation bug lands in it looking plausible — and the first characterisation attempted, *a live member and nothing else*, was plausible, tidy, and **wrong on seven combinations**. Stating only the positive form would claim the code tests a condition it does not test. **One session measurement is refreshed** in `lifecycles.md`: 11 sessions to **12 — 5 `closed`, 3 `active`, 3 `handoff`, 1 `available`** — with the note that one of the nulls is a session that closed by crossing. **Numbers, none of them an `OK` total.** Seven files changed — six documents and this manifest — **+320, −32**; the six documents are **+318 −32**, all prose: no code, no record changes shape, no test added or altered. `counts` **FAIL 0**; `headers` **11 FAIL** in `0030` and `0035`, unmoved and now cited by §9c as its evidence; `structure` **FAIL 0**; completeness **0 problems**; suite **77, all passing**; `check` **unmoved at 26**; doc-paths clean on the default run and **MISSING 24 unmoved** on `--all`; portability **WARN 0, FAIL 0**; `stamp --dry-run` writes **0 records**; coverage MISSING 0, ORPHANED 6, DUPLICATE 1, unmoved; and **doc-currency 7 watches, 0 drifted, 7 never confirmed — identical either side**, which is `0039` F22 standing rather than anything this revision moved. Measured in a Linux VM on the owner's machine under Bash 5.1 and Python 3.10, **not the macOS Bash 3.2 target**.

**Revision 314** — supersedes Revision 313 and earlier. **A record write: `docs/ideas/big-picture.md` gains the architecture review, a layer that had no section, a census, and a design order.** Owner-directed throughout and carrying no member, so §6's override applies and this entry says so. **One document changes and nothing executes differently.** **What a layer is, stated before any are listed**: *a layer is a behaviour of the running system — remove it and the system behaves differently; a document is a description of a layer — remove it and only a reader is worse off.* **The first pass of the review failed its own test** and is recorded doing so: it listed `vocabulary.md`, `schema.md` and `lifecycles.md` as layers when the Vocabulary *layer* is the nine closed sets in `plan_findings_work.py` and the guard that holds them disjoint. **Five categories are named for what is not a layer** — Law, Cartography, Measurement, Production, Orientation — because *rules* and *build artifacts* had no home and kept being promoted: **`iris/rules.md` is Law, which is the input to Permission and Verification and is neither**, and the gap between *the rule exists* and *something enforces it* is what collapsing them hides. **Nine layers, each measured rather than recalled.** Vocabulary is the only finished one. **Record is split**: every derivation runs off **53 `metadata.json` files** and the **94 markdown record files feed no derivation at all**. **Derivation is correct as of Revision 313 and `stamp` has no automated caller anywhere in the tree** — its only invoker is `bin/plan-findings-work.sh`, which a person runs. **Act is specified and unbuilt**: `crossing` and `declaration` appear in **three docstring lines and one document**, **zero code branches on either, zero records carry `by`** — and Revision 313 carries the layer's exhibit, a session closed by crossing that stayed in the assignment pool because the filter sampled two of five inputs. **Two claims in the previous draft of this review were wrong and are corrected here rather than quietly dropped.** `.claude/hooks/` **does** contain a real gate: `write-location-guard.sh` is PreToolUse and **denies** a write into the owner's checkout, and `session-guard.sh` and `runbook-guard.sh` fire PostToolUse on every Edit/Write and name the governing rule. So the *before* and *immediately after* rows of the four-position table are not empty. **The machinery exists and is bound to the wrong act** — it observes *an agent edited a file*, not *a member's status moved* — **and it lives in `.claude/`, so it protects one assistant.** That is a cheaper problem than an absence and the table now says so. **And the doc-graph hub is `docs/legend.md` at in-degree 16**, not `iris/vocabulary.md` at 11 as previously stated; the earlier figure was measured over `iris/` alone. **Permission's data is weaker than its rules.** `writeCategory` appears in **zero records** and one architecture document; the class is derived by **substring match against a free-prose `scope` field**; and **12 of 41 dossiers have `scope: null` and all 12 land on `record`**, the least-gated class. **Two-thirds of that class is assigned by absence of data.** **Allocation gains both instruments by name.** **Prism** proposes and never assigns, and reads a pool with no clock — four assignable sessions last wrote **09-13, 09-10, 09-09 and 09-08** and nothing in the data says so. **Lumen** schedules the owner's attention, which Prism's own document calls the actual bottleneck, and **13 of its 14 design elements have no code**; `readiness` is on **0 of 232** members and `severity` on **0 of 232**, which are two of the three tiebreak keys, so a run today returns **four candidates at an identical 26.10** in the shape of an answer. **And `ask` writes nothing**, where `§5.2` designs the interview as the thing that produces `decisions.md` with its rejected alternatives — the Act layer missing at the one moment the design most depends on it. **Units of Work is promoted out of `docs/architecture/typed-bundles-and-work.md`**, which marks itself *a draft to be worked* and is still the only recorded design. **Three namings are live for two categories** — `reasoning`/`actionable` in code, `reasoning`/`doing` in the architecture record, `Reasonables`/`Actionables` from the owner — **and `shape` carries two unrelated senses**, the category and the structure of a JSON object. **`bundle` is retired in this document.** The collective name is left open with `Matter` leading and the constraint recorded: **Session is the only one of the three that acts**, so every `-able` form quietly excludes it. **Measured: `serves` is not among the tree's edge kinds, one commission exists and is unclaimed, and no Actionable has ever been created** — `charter` and `remedy` are in `GENERA` with no instances. **A Current-State Census of all 493 tracked files**, in four divisions: **IRIS System 38, IRIS Workspace 288, IRIS AI Files 18, Not IRIS 149.** **The System is 7.7% of the repository.** **`docs/legend.md` is System living in Workspace** — the most-cited document in the tree, sitting in the records directory, which is a large part of why the standing derivation is restated in six live documents. **`prism.md`, `lumen.md` and `status.md` are mechanism and measurement in one file**, both instrument manuals dated 2026-09-09 and now stale — Prism states 54/191/59 against **57/232/79**, Lumen 191/29 against **232/47**. Findings are grouped into nine topics; sessions are audited individually and **`0043` is owned by two sessions at once**, `ended.on` holds the literal string `"unknown"` twice, one `handoff` holds eleven dossiers with no successor, and the session record shape runs from 4 files to 13. **Gaps are listed as files that do not exist**: four missing blueprints, three missing record kinds, seven missing pieces of code, and a machine-readable vocabulary register. **Three measurements already on the page are corrected against the whole tree.** The field census **silently excluded `docs/runbook-findings/`** — 16 dossiers and 25 members — and the exclusion is now stated as a judgement to ratify rather than inherit. **The migration arithmetic did not match its own terms**: *"~260 records — 12 sessions, 41 dossiers × 2 dispositions, 207 members"* gives 301, and over the full population it is **12 + (57 × 2) + 232 = 358**. Member statuses undated move from 93/207 to **118/232**, dossier standings from 0/41 to **0/57**. **And one correction went the other way.** This session measured `SESSION.transcript` as 1 of 12 by counting files and was about to overwrite `drift-and-the-write-boundary-20260909-053548`'s 11/12. **The field holds a session URL, not a file** — Drift's figure is right, and the row now records both: **field 11/12, saved transcript file 1 of 12**, which are two facts and only the first was ever being measured. **A provenance design, and the owner's decision that IRIS becomes its own project.** Today a member carries a **singular `resolution`** with four fields — `resolvedBy`, a free-prose `whatWasDone`, `revision`, `commit` — and **no path, no line, no realm**, so neither direction is answerable: *which files did this member write*, and *which member wrote this line*. **The nearest existing thing is `contributions` and it is unshaped** — 24 entries across 8 of 57 dossiers carrying **two different key sets**, one of whose `date` fields holds *"item 1 of the reading order (`prompt.md:25`)"*. **The design is a schema-backed JSON file beside `metadata.json`, not a ledger and not prose.** Not `docs/ledgers/`, because a ledger here is *a dated measurement of one class, repeated* — **never a verdict, always a value** — and this records one act; reusing the word would collide the way `charter` and `shape` already have. **Beside `metadata.json` rather than inside it on three measured differences**: it grows with every act forever where `metadata.json` is bounded, it is read only by provenance queries where `metadata.json` loads 57 times on every `check`, and it is **append-only** where `stamp` rewrites `metadata.json` — **an imprint is evidence of an act and editing one is falsifying a record**, the same property `findings.md` has. **Line numbers rot, so a range is never stored alone**: `range` **plus `commit`** is permanent and reproducible with `git show`, and `anchor` answers *where is it now* and is allowed to drift **detectably**. **`commit` is null in 74 of 107 resolutions** — **a line number without the commit that pins it is not a citation, it is a guess** — which makes filling it a prerequisite rather than a nicety, and it is added to Tier 0. **`realm` is `project` or `iris`, stored and not inferred**, which makes *only work on IRIS if you are in the IRIS project* **the first permission rule in this system with an actual test behind it**; every other one is convention or a hook matching a file path for one assistant. **And it must be backed by a schema, which would be the first in the tree.** Measured: **no JSON Schema exists anywhere**, and **all 69 records carry `schemaVersion: 1`** for a schema that lives only as prose in `iris/schema.md`, whose own header says the code is the tiebreak — **so `schemaVersion` versions nothing checkable**, and `contributions` in two key sets is what that costs. The file therefore ships with a committed schema, a `schemaVersion` that **refuses rather than guesses** on a version it does not know, and closed sets **referenced rather than restated**, so `realm` and `verb` do not become the seventh copy of a vocabulary. **It is written to be read by audit scripts**: one loader and one home for the read — `0050` F11 is the precedent, where a fourth copy of `bundle_standing` disagreed with the module for four revisions while every run said `FAIL 0` — with the **reverse index derived and never stored**, and five checks named as the test of whether the shape is right, including **a decision `accepted` whose members resolved with nothing written anywhere**, which is `0012` D2's 115-revision failure mode as a query. **Where Is Home is decided.** IRIS becomes **its own project**, started fresh with some files ported, **released under a version** that projects consume, and **worked on only in the IRIS project**. **The census is the port manifest** — System 38 the candidate set, Workspace 288 staying as this project's memory, **AI Files 18 the ones that need deciding** because IRIS's rules reach a session only through them — **and that sort is a judgement to ratify rather than inherit.** The split settles what nothing else could: **`0053` becomes a version comparison** instead of a global unknowable, **`0057` largely dissolves** because mechanism and measurement separate by construction, **`legend.md` leaves the records directory by moving rather than by being renamed**, and **an IRIS upgrade inside a project becomes an Actionable** — the first real use of the category. **A Design Strategy in five tiers**, ordered by dependency and **not by migration cost, which the owner ruled out as a criterion.** **Tier 0's first job is the two directory structures** — freestanding IRIS and IRIS resident in a project — with the realm boundary and the release model, on the owner's ruling and for the reason that every later artifact is written into one of the two. **Then it settles the words** — an exhaustive register of every closed set, its values, every document that states it and every code site that enforces it, because *a thing that later documents restate must be settled before they are written*, and six copies of one derivation rule is what not doing that looks like. **Tier 1 is the disposition triple, and its internal order is inverted from the obvious one**: the verifications are written and **shown to fire on a case they should catch** *before* the retrofit runs, because the retrofit is where the missing values get determined and that is safe only if something can say when one was determined wrongly. **Tier 2 makes `state-as-data §6.1` execute.** **Tier 3 re-binds Permission and adds act-bound verification. Tier 4 is Units of Work and then the consumers.** **A global watcher is deliberately excluded**, on the evidence that `verify-manifest-coverage.sh` is correct, fires correctly and is invoked by nothing. **The commission this bootstraps is named as its own kind**: not greenfield, but a re-architecture of a system already far along and load-bearing, whose inputs are a census and a vocabulary register and whose outputs are blueprints. **The vocabulary has no word for that kind**, and Tier 0 owes one. **Pending: the release patch.** A composed and unapplied patch — `R314-release-and-the-unclaimed-cell.patch`, `instruments-and-blind-spots-20260909-220203`'s — releases `0041` and `0050` to `unclaimed`. Applied to a scratch checkout of `f48a17c` it moves exactly four figures in the census and nothing else — **unclaimed dossiers 18 → 20, live members inside them 27 → 48, unclaimed cost 81.2 → 103.6, and session states from `active` 3 · `available` 1 to `active` 2 · `available` 2** — with the **same four assignable sessions**, `check` unmoved, and the double-ownership and claimed-but-unclaimed counts unchanged. **The census states its own date rather than smoothing this**, which is the discipline the two corrections above are about. **This revision takes 314 because the number is taken at apply time and the release patch had not been applied**; that patch is named for 314, is now **315**, and its entry's *supersedes Revision 313* becomes *supersedes Revision 314*. **No file this revision touches is touched by it** — it cites `docs/ideas/big-picture.md` and does not modify it — so the two are independent apart from this manifest, and it applies cleanly on top of this one. **Numbers, none of them an `OK` total.** Two files changed — the document and this manifest — **+1,152, −24**; `docs/ideas/big-picture.md` is **+1,150 −24** and **every one of the twenty-four deletions is a rewrite of a line this revision replaces**, listed and checked one by one, the last of them the absent trailing newline. No code changes, no record changes shape, no test is added or altered: `counts` **FAIL 0**; `headers` **11 FAIL** in `0030` and `0035`, unmoved; `structure` **FAIL 0**; completeness **0 problems**; suite **77, all passing**; `check` **unmoved at 26**; doc-paths **clean on the default run**, and on `--all` **MISSING 24 before and 24 after, unmoved**, with **OK 2700 → 2733** — this revision adds thirty-three resolving references and not one that does not resolve; portability **WARN 0, FAIL 0**; `stamp --dry-run` writes **0 records**. Measured in a Linux VM on the owner's machine under Bash 5.1 and Python 3.10, **not the macOS Bash 3.2 target**.

**Revision 313** — supersedes Revision 312 and earlier. **Owner-directed throughout, and the whole revision is under the override of §6.** Every change here was decided by the owner across a design conversation and has no member behind it; §6's case is exactly *a change the owner has already decided, with nothing to discuss, which the lifecycle would delay without adding anything*, and its whole discipline is that the revision says so. **Two gates in §0, and the composing one was missing.** The step read *where an instruction could be either, compose and ask*, on the reasoning that *composing when an apply was wanted costs one exchange*. **That cost is understated and the bias is backwards**: composing early costs the owner the ability to add to the revision and produces a patch built against a specification still being given — **which happened while this conversation was in progress**, and the patch had to be rebuilt. §0 now states two gates that neither implies the other and neither is satisfied by inference: **no composing until the owner says so, no applying until the owner says so**, and *an owner still talking is not an owner who has finished asking*. **A `closed` session may not write.** Not its own record, not a dossier, not the toolkit — a state that can still change the tree is not terminal. The exception is §6's override, named in the revision that carries it, and **the remedy is ordering rather than permission**: everything closing owes must be written while the session is open, which means before the last member resolves, because that resolution is what closes it. **A final summary written after the crossing is already an override.** **`live_sessions()` becomes `assignable_sessions()` and computes the state.** It tested `ended.on` and `declaredState` — **two of the five inputs `session_state()` uses** — so any state reachable without touching those two was invisible to it. That is a class rather than a bug, and the instance that occurred was a session closed by **crossing**: every owned dossier terminal, no latch, no declaration, reading `closed` and still in the pool. **Reading the stored `state` was rejected**: `allocate` does not stamp, so an unstamped tree would mis-allocate silently, and it would add an unwritten *run stamp first* convention. Computing needs no stamp and uses all five rows, so **`stamp` and `allocate` now agree by construction rather than by discipline.** Measured on the live tree: **assignable sessions 5 → 4**, the removed one being this session, and with no latch set. **Three bands are named in code**: `INERT`, `TERMINAL`, `ASSIGNABLE`. A **band** is a named subset of one disposition's values — not a disposition and not a value — and it is the word rules and checks kept needing without having. **`un-started` is deliberately not inert**: `INERT` means *no work remains here* and `un-started` means *all of it does*; the measurement is that including it would price six dossiers at zero in `cost()` and drop **31 of 116** open members from the allocator's count, silencing exactly the rows `0047` F3 and F13 exist for. **`bundle_is_terminal()` becomes `dossier_is_terminal()`** — `bundle` described how a dossier is stored rather than what it is. **`declaredState` is constrained and refuses rather than passes.** Four documents gave four answers to what may be declared — `lifecycles.md` §4 says one, `procedure.md` §8 implies two, the old filter named three, and `session_state()` accepted all five. The split is **declarable `handoff` · `dissolved` · `closed`**, **derivable `available` · `active` · `closed`**, with `closed` in both because the owner may close a session with work outstanding and no derivation produces that. **Declaring `available` or `active` now raises**: it would store a derivable value, force assignability regardless of what is owned, and `stamp` would launder it into `state` as though derived. **That makes the band airtight** — neither assignable value can be declared, so assignability follows from what a session owns and its two terminal signals. **Three tests added and one renamed**, suite **74 → 77**: a session closed by crossing is not assignable, an undeclarable state raises, and the three bands are asserted as subsets of their vocabularies with `un-started` explicitly outside `INERT`. **§10a requires the date.** Every disposition transition records when it happened, and a release leaves the least behind — the manifest row is gone and the index row names nobody — so without a date the reading's history has no last timestamp at all. **`docs/ideas/big-picture.md` gains three sections**, at the owner's direction and merged into their structure: the **disposition** vocabulary and the `{value, by, on}` shape with its write rules; the measurement that motivates it — **93 member statuses undated, 0 of 41 dossier standings, 0 of 14 supersessions, 0 of 12 session states**, and `updatedAt` unable to serve because it moves when prose is edited; and the field census — **five fields populated nowhere in the tree**, 74 resolutions naming no commit, 29 edges asserting a relation without saying why, and 21 decisions citing no member, **which is `check`'s standing `UNCITED-DECISION (21)` — the row and the null are one fact.** Nothing in those sections is decided. **This session's own closing record is written under the override**, carried from the composition the owner stopped: `ended` with its reason, revisions and commits **derived once and corrected by hand** — the trailer gives 16 and is wrong twice, both `0050` F7's instances — `owners[].until`, and a `final-summary.md` that records what the session got wrong as well as what it did. **`0038` D11 is amended, not rewritten**, after Revision 312 showed one sentence conflated *cannot write §0 or §6* with *cannot decide*. **One collision caught by a numbering check rather than by reading.** This revision drafted a §9c for closing a dossier — `0047` F13's remedy, which the coordination boundary assigns to this session — and **§9c already existed**, written by `entity-model-and-vocabulary-20260909-053548` after this session handed it over at Revision 300 on a boundary reading that was wrong. **The duplicate was removed rather than merged**: a second section is worse than none, and editing theirs is not this session's. **What is handed over and not written here**: §5's declaration preconditions for `closed`, `handoff` and `dissolved`; §10's transfer date and assignable-target rule; §9's supersession date and successor-created rule; and every legend and `vocabulary.md` entry for the vocabulary above — all `entity-model-and-vocabulary-20260909-053548`'s. **Numbers.** `counts` **FAIL 0**; `headers` **11 FAIL** in `0030` and `0035`, back to baseline after this revision's own §10a addition briefly made it 12 by indenting a paragraph into a code block; `structure` **FAIL 0**; completeness **0 problems**; suite **77, all passing**; `check` **unmoved at 26**; coverage **MISSING 0, ORPHANED 6, DUPLICATE 1**; doc-paths **0 MISSING, 0 DECAYED**; portability **WARN 0, FAIL 0**; `stamp --dry-run` writes **0 records**. Measured in a Linux VM on the owner's machine under Bash 5.1, **not the macOS Bash 3.2 target**.

**Revision 312** — supersedes Revision 311 and earlier. **A findings bundle must be able to reach a terminal standing, and `0041` could not — which is how a call two sessions had argued and agreed turned out to be wrong.** The owner named the test. Under the disposal recorded at Revision 304, `0041` F7 was settled, correct and permanently `framing`; because `framing` is not inert, **resolving every other member of the bundle still derived `analyzing`**. The bundle was barred from `answered`, `retired` and every terminal value — not by outstanding work, but by a member correctly disposed of in a way the vocabulary cannot express. **Nothing in the tree could have reported it**: the data was internally consistent, `stamp --dry-run` wrote nothing, every projection agreed with its source, and `conformance` was clean. It is not a drifted value but a reachable state that was unreachable. **`0041` D8 reverses the disposal.** §11's rule turns out to be a **dangling-citation rule, not an ownership rule** — *every `D<n>` a resolution resolves by exists in `decisions.md`*, because *a citation that resolves to nothing is how a table stops being a record and becomes decoration*. Revision 304 read it as barring this bundle from answering F7 at all; it bars one thing, naming another bundle's decision in the `Resolved by` column. **Measured: 117 resolution rows across the tree — 97 name a local `D<n>`, 19 carry an em dash, one names a range, and not one cites another bundle's decision.** The convention is universal and D8 does not break it. **`0038` D11 is the worked precedent**, one day old: *`0012` D2 is adopted as F12's answer rather than cited as it* — and that bundle could not perform `0012` D2's act either, since it was the owner's and no patch could have contained it. **D11's own text carves this case out** on the grounds that *the finding's bundle cannot make the decision*, **and that conflates two abilities**: `0041` cannot write §0 or §6, but it owns F7 and deciding is the one thing an owner may always do. **So `0041` adopts as F7's answer the remedy carried out under `0038` D10 (§0 step 3) and `0039` D28 (§6's recipe block)** — and **verified it present at `4b0c95f` rather than taking it from the decisions claiming it**: §0 step 3 at line 53, §6's block at line 595, both reading `git add -N .` then `git diff HEAD`, with the superseded `git diff >` form surviving nowhere. **F7 `resolved`; the bundle can terminate.** **`0041` F9 records what D8 does not repair.** Nothing asserts that a bundle can reach a terminal standing, and it is not fully derivable — whether a member can leave `framing` depends on who owns what and what the rules permit, which is not a field. **The detectable half is sharper**: F7's disposition lived only in prose, with `statusNote` and `statusReason` `null` for eight revisions, so `stamp`, every checker, both index rows and the session manifest read a bare `framing` indistinguishable from unworked while the paragraph beneath it said the matter was closed and agreed. **A member carrying its disposition in prose and `null` in its status fields is a checkable shape.** **`0050` F12** is a hand-off that vanished: Revision 301's entry states that `verify-findings-headers.sh` *cannot tell a cross-bundle citation from a local one* and was **handed to `0050`** — and it was never written into the bundle. Measured in three forms against a baseline of `FAIL 11`: a bare mention, a backticked bundle number, and an unbacktracked one all give **FAIL 12**. The prefix makes no difference; the checker reads any `F<n>` token anywhere in a resolutions row as a local citation. **Decisions in prose are unaffected** — the defect is specific to `F<n>`, which is the token a resolution most needs when the remedy came from elsewhere, **so an instrument that fires on correct work is cheaper to reword around than to fix, and every past instance was silently reworded.** **`0041` D8's resolution row applies that workaround in the same revision that records the defect**, naming the two decisions and deliberately not their findings. **The Revision 304 paragraph is left standing in `findings.md` rather than deleted, because the reasoning is the finding.** Baselines: `counts` FAIL 0, `headers` FAIL 11 unmoved, `structure` OK 121 FAIL 0, completeness 0, suite 74, `stamp --dry-run` writes nothing, doc-paths 0/0.

**Revision 311** — supersedes Revision 310 and earlier. **The rollover that made the manifest readable made one instrument blind, and the session closes.** Three things, and the middle one was not asked for. **`0038` F12 is `resolved` against D11, and its remedy is 115 revisions older than the finding.** `0012` D2, accepted 2026-09-04 — *“The empty directory is removed locally, and that is not a repository change”* — was never carried out; `0012` F1 resolved against D1, the bundle went `answered`, and **D2 sat undone and invisible, because the only tree in which it mattered was one person's.** `.internal/restore/` was empty, untracked and not gitignored, so ten citations resolved for the owner and broke for everyone who cloned. **The owner removed it at Revision 310** and the trees agree for the first time: `verify-doc-paths.sh --all` reads **24 in the checkout and 24 in a clone**, where F12 recorded 10 against 20. **The disagreement was compounding**: citations of that path went from **10 to 17 across four revisions**, over five documents, because every document written about the defect cited it — this session's own §6 rule among them, which is `0041` D1's matcher problem in a sixth form. **Removing it repaired nothing and revealed everything**, which is correct: the citations were already broken and the leftover directory hid that from the one person who could see it. **D11 adopts `0012` D2 rather than citing it**, because §9b requires `Resolved by` to name a decision in the finding's own bundle and **a resolution pointing at another bundle's decision is a citation dressed as ownership** — the disposal this session recommended to `0041` for F7, now applied to itself in the direction that costs it something. **`0052` gains F3, recorded and resolved, and it is Revision 310's own consequence.** Rolling `APPLY-MANIFEST.md` over at 1,312,633 bytes and 468 entries was right — `0055` F1 measured it growing 8,162 bytes a revision on every session's read path — **and the roll is not the defect.** The defect is that two instruments read that filename and only one survived: `.share/check-manifest-revision.sh` returned **311** correctly, and `bin/verify-manifest-coverage.sh` went **MISSING 0 → 79**, reporting every archived entry as a revision written nowhere. **The instrument was not wrong about anything it could see**; its subject had been split in two and it was told about neither half. **The survivor survived by accident**: Revision 278 gave the number helper the commit log as a third place, for `0047` F12 — a number taken in a commit message — and that widening, which had nothing to do with rollovers, is the only reason numbering worked five minutes after the roll. **This is F2's subject with the plan missing rather than the rule.** §6's migration gate asks *what happens to what predates this*; **a rollover makes no record non-conformant, so the gate does not fire, and what breaks is not a record but a reader.** Nothing asks *who reads this path*. **D3 reads the set.** Coverage now collects every `APPLY-MANIFEST*.md`, greps the set, and passes the same pattern to the `git log` pathspec that finds which commit introduced an entry — an entry never leaves the set, only the current file. **Verified in both directions**: against the tree it reports **MISSING 0, ORPHANED 6, DUPLICATE 1**, **identical to the last figures taken before the roll** rather than merely lower, which is what distinguishes a fix from a suppression; and against a constructed case — one entry removed from the archive in a throwaway copy — **MISSING goes to 1**. Rejected: naming the archive, which works today and fails at the second rollover; a pointer file naming the current manifest, a second copy of what the filesystem holds; and treating it as `0050`'s, whose subject is instruments that cannot fire, where this one fired correctly on a subject halved underneath it. **The rule half is recorded and not built**: a rollover owes the naming of its readers, and that belongs to §6 and §7. **And `docs/ideas/big-picture.md` gains a Detection and Verification focus group**, at the owner's direction and merged into their structure rather than appended. **Its claim is that `IRIS` verifies documents and has never verified an event**, and its evidence is the measurement of 2026-09-11: five projections disagreed with their source across 613 rows and **not one was found by an instrument** — `verify-findings-structure.sh` passed 70 of 70 while a session manifest's copy of a standing was wrong, being pointed at a file rather than at the event *a standing moved*. It records the four positions a check can occupy — before, during, immediately after, eventually — notes that **`IRIS` has only the last**, and argues the cheapest first move is making `state-as-data.md` §6.1 executable, it being already the derivation graph with **zero generated regions in the tree**. It states one objection rather than burying it: **an all-seeing watcher is the shape that already failed**, `0050` F8, and the actor knows what it changed where a watcher must infer it. **Nothing in that section is decided**; it is an ideas document and the questions are the owner's. **One record write, one toolkit write gated on F3 being `decided`, and no migration plan** — nothing changes shape and no field is added, renamed or retired. **Numbers, none of them an `OK` total.** `verify-manifest-coverage.sh` **MISSING 0, ORPHANED 6, DUPLICATE 1**, restored; doc-paths default set **0 MISSING, 0 DECAYED**; portability **WARN 0, FAIL 0**; `counts` **FAIL 0** at **21 bundles · 147 findings**; `headers` **11 FAIL** in `0030` and `0035`, unmoved; `structure` **FAIL 0**; completeness **0 problems**; suite **74, all passing**; `check` **unmoved at 26**. **Projections swept both classes before handing over**, the thing this session drifted twice: session-manifest `Standing` **0 of 37** and `docs/sessions/INDEX.md` `State` **0 of 12**. **`stamp` wrote two records and the second closes this session**: `0038` to `answered`, and `drift-and-the-write-boundary-20260909-053548` to **`state: closed`** — all three bundles answered, `0038`, `0047` and `0052`, with `0055` recorded `unclaimed` and owned by nobody. Measured in a Linux VM on the owner's machine under Bash 5.1, **not the macOS Bash 3.2 target**.

**Revision 310** — Created a new APPLY-MANIFEST.md because the original one was getting too big. I removed `.internal/restore` so 0038 F12 is genuinely resolved. Drift, otherwise known as session id `cse_01H9nWPECCmRoZDipJSYA2iS` and also goes by the alias `drift-and-the-write-boundary-20260909-053548` will followup and update the status accordingly.
I've added `docs/ideas/big-picture.md` to zoom out and carefully craft an overview of `IRIS` to pivot to a different strategy using new sessions some of which will be specialized.