# Handoff — session-management re-evaluation

**Written:** 2026-09-08 10:00 EST  
**Session:** `session-management-re-evaluation-20260906-110105` (`session_01FhFbEgG4wmrtJqUCcryNVQ`)  
**Handed off at:** Revision 221, composed against `3828d19` (Revision 220)  
**Environment:** Linux VM on the owner's Mac — Bash 5.1.16, GNU coreutils, aarch64. **Not macOS.**

The session is handed off rather than closed: it still owns seven bundles, and
`0037` D2 and the `0039` toolkit installs are owed. A successor picks up the
manifest as it stands.

## What this session did

Re-evaluated the six bundles released by `restore-apps-outstanding`, recorded
`0043` and eight new findings across the set, designed the JSON state format,
repaired the instruction set under an owner override, and — in its last sitting —
gave the finding lifecycle the transition rules it never had.

## The four things a successor most needs to know

**1. A status moves on a material change to the record, never on a reading.**
`0039` D7. This session broke it before writing it: Revision 208 flattened
twenty-one `reopened` rows to `framing` while believing it had only read them,
and every validator passed. The reading is `0039` F12.

**2. `reopened` and `superseded` are not alternatives, and the difference is an
address.** `reopened` requires the finding itself to have been `resolved`.
`0038/F1` never was; `0028/F1` was, and that is a different bundle. The six
bundles `0036`–`0041` are supersession successors, so their findings are
`framing` and their relationship to their predecessors is **provenance** — 31
edges, derived by comparing statements, coverage and exclusivity passing on all
six pairs. This session proposed the wrong repair first; the correction is
recorded in F12 rather than quietly applied.

**3. The JSON exists but nothing reads it yet.** 54 `metadata.json` files, one
per findings bundle and session, extracted one way from the markdown. The
markdown is still authoritative. Step 2 of `state-as-data.md` §9 — regenerate the
markdown from the JSON and diff — **has not been done**, and until it has, the
extraction is unproven in the direction that matters.

**4. Nothing here ran on Bash 3.2.** Four scripts have new self-location lines at
depths they have never been executed at, and `extract-metadata.py` has run
exactly once, on Linux. `/bin/bash -n` on the target Mac is owed for these and
for Revisions 116 onward.

## Owed, in the order it should be picked up

| # | Item | Why it is not done |
|---|---|---|
| 1 | Install `0039` D7–D12 in the remaining procedure text | Decided 2026-09-08; the legend and §9/§9a carry them, the runbook-facing prompts do not |
| 2 | `0037` D2 `Outcome` → `rejected` | Owner rejected it 2026-09-06; owed whenever `0037` is worked |
| 3 | `state-as-data.md` §9 step 2 — regenerate and diff | The extraction's only real proof |
| 4 | `0044` assignment | The owner's act |
| 5 | Four architecture records with run-on headers | `restore-docker-teardown-and-test.md`, `restore-repos-clone-plan.md`, `sign-off-consolidation.md`, `time-machine-run-index.md` — toolkit side, flagged not fixed |
| 6 | `.github/session-management-instructions.md` four-space indented example | Breaks §11's own rule; flagged, unfixed |

## What was checked, and what was not

**Checked, on Linux:** all six validators; `bash -n` on five shell scripts;
`py_compile`; 54 files parsed as JSON; a fence-and-table render check across every
changed markdown file, which found three table-splitting blank lines — one in this
session's own `findings-manifest.md`, two in `0039`'s decisions table, which had
been rendering as four separate tables.

**Not checked:** anything on macOS or Bash 3.2. The render check is a throwaway
script in scratch and is not committed — no checker in the repository reads a
rendered page, which is `0042`.

## Bundles owned at handoff

Seven, 44 findings. `0036` F1 `decided`, `0039` F7 and F12 `decided`, `0043` F5
and F6 `decided`; the rest `framing`. `0042`, `0044` and `0045` are not this
session's.
