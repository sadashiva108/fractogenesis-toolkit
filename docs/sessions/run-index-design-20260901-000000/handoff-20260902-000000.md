# Session handoff — run-index design and evidence conformance, 2026-09-02

Session `01KcZvrKMgfenhrT9DvxW9Jk`. Companion to
[[docs/sessions/phase-11b-hydrate-and-bookends-20260903-141500/handoff-20260902-000000|the Restore Repositories Refactor handoff]],
which ran concurrently against the same tree. Where they overlap, that one owns
Phase 11B and this one owns the run index and evidence conformance.

Picks up from [[docs/sessions/run-index-design-20260901-000000/handoff-20260901-222913|the 2026-09-01 handoff]]. Brief:
[[docs/sessions/run-index-design-20260901-000000/prompt|next-session-prompt-run-index.md]], items 1–7.

**Items 1, 2 and 3 are done. Item 4 is next.**

---

## 1. What shipped

| Revision | Change | State |
|---|---|---|
| 129 | The Phase 6B repository-audit row stops failing a manifest that is exactly correct | committed |
| 130 | The documentation lint stops counting the notes nobody ships | committed |
| 132 | Post-image Time Machine evidence has one home; the empty second one is gone | committed |
| 133 | `docs/architecture/` and `docs/ledgers/` exist, and a fresh clone gets them | committed |
| 134 | The Phase 11B sign-off joins every other post-image answered row | committed — `8015e74` |

Revision 131 is the other session's.

**The working tree is clean. Nothing is waiting on you.**

### One thing to know about Revision 132

Its **changes** were swept into the other session's commit `9b6f67a`, which
describes only the Phase 11B clone-list work; its **entry** was committed
separately afterwards. Nothing was lost. It is the second time this has happened
(`b3493e5` was the first), and the cause is the same both times: two sessions
editing one tree, one of them staging while the other is mid-write.

## 2. Decisions you made

Recorded here because they are the thing a future session would otherwise
re-derive from a chat log.

| # | Decision | Where it lives |
|---|---|---|
| — | The boundary-recorder chain **closes at both ends** — 10B checks that 10A finished, and Phase 12 gets an exit it can actually record | [[docs/*-findings/boundary-recorder-coverage-is-uneven\|gaps/boundary-recorder-coverage-is-uneven.md]] |
| — | `reimaged-system/time-machine/` **deleted**; the root-level `time-machine/` holds both phases like every `capture-` category | Revision 132 |
| D1 | Pre-image uses `<category>/sign-offs/` consistently; `manual/` and per-category `checklists/` retired as homes for answered rows | [[docs/architecture/sign-off-consolidation\|architecture/sign-off-consolidation.md]] |
| D2 | **No mixed-mode artifacts.** Automated output and answered rows are always separate files | same |
| D3 | Closed pre-image artifacts **may be split**, backup first. Structure may move; content may not | same |
| D4 | Runbook code blocks stop authoring artifact-root notes and inventories | same |
| D5 | The pre-image / post-image structural asymmetry stands for now — documented, not changed | same |
| D6 | Capstone checklists **are** run-indexed | same |
| — | Time Machine: **option D**, and **migration (i) — migrate everything** | [[docs/architecture/time-machine-run-index\|architecture/time-machine-run-index.md]] |
| — | The `final` checklist is **neither a checklist nor a sign-off**. It splits into a run named `<phase>-evidence-summary` and a sign-off | same, §9 |

### Two that changed a previous answer

- **Item 7's "captured data is not to be modified" is relaxed** by D3, for
  structure only. Several plans were written under the old constraint and their
  recommendations moved as a result — the Time Machine migration most of all.
- **The `final` roll-up cannot rescue option C.** It looked like it might: it is
  the one artifact naming four other artifacts in a single table. It cannot,
  because identity is assigned when a run is written and the roll-up is produced
  afterwards, and because it covers four of the nine kinds — not `logs` or
  `diagnose`, which are the two C misfiles.

## 3. Where the deliverables are

- **[[docs/ledgers/evidence-conformance|ledgers/evidence-conformance.md]]** —
  four tables across the whole artifact root: never run, stale, needs refactor,
  already refactored. Plus irrecoverable-data callouts, pin candidates,
  decisions-log coverage, and the sign-off extraction list. **Table 3 is item 4's
  candidate list, already ranked by cost of waiting.**
- **[[docs/architecture/time-machine-run-index|architecture/time-machine-run-index.md]]**
  — item 3, settled and approved, no open decisions. Replaces the 2026-09-01
  record, now retired; its unique paragraphs are carried in *History* there.
- **[[docs/architecture/sign-off-consolidation|architecture/sign-off-consolidation.md]]**
  — the six decisions above, 35 mixed-mode artifacts inventoried, the split
  procedure and its backup, the inline-generator conversion list, the capstone
  run-index design, and a seven-step order. Step 1 is done.
- **[[docs/sessions/run-index-design-20260901-000000/prior-phase-evidence-map|prior-phase-evidence-map.md]]** — item 2's map and
  the conclusions that are *not* gaps.
- **[[session-responsibilities|session-responsibilities.md]]** — the boundary
  between the two sessions. Start there.

Eight gaps were filed and one closed; see [[docs/*-findings/INDEX|gaps/INDEX.md]].

## 4. What is next

**Item 4 — run-index coverage audit.** Table 3 of the ledger is the candidate
list: `performance-audit/`, `office-stability/`, `toolkit-snapshot/`,
`loose-secrets-reports/`, `size-audit-reports/`, `reimage-prep-checks/`. Three
carry their own question:

- `toolkit-snapshot/` is the last category using **symlink** pointers, which
  cannot express first-wins, cannot be pinned, and do not survive a copy.
- `loose-secrets-reports/` and `size-audit-reports/` look converted and are not —
  no `official/`, and foreign manifest headings, so
  `_artifact_runs_ensure_manifest` will **refuse** to append. They need the
  Revision 120 rename treatment first.
- `office-stability/checklists/` holds evidence bundles that are really runs, and
  its producer emits manual items as `record_check WARN` rows inside the
  automated table.

Read [[docs/cross-cutting-findings/0009-dated-artifacts-cite-run-ids-a-rename-breaks/findings|the rename note]]
first: every conversion here is a rename, and a rename breaks the citations
already written against the old name.

Then items 5, 6 and 7 in order.

**Three gaps want a revision of their own**, independent of the item order:
`recorder-usage-strings-understate-supported-runbooks.md` (one revision covers
both scripts), `boundary-recorder-coverage-is-uneven.md` (Decision 1 answered,
Decision 2 waits on `restore-apps.md` being finished), and
`restore-access-exit-predates-its-own-state-walk.md` (a re-run, not an edit).

## 5. Nothing was refactored on the volume

Worth stating plainly, because this session produced a lot of design.

`time-machine/` is untouched: the same seven flat items from 2026-08-17, no
`runs/`, no `MANIFEST.md`, no `official/`, no `sign-offs/`. Neither producer
sources `.internal/artifact-runs.sh` yet. The only change to the artifact root
all session was deleting the empty `reimaged-system/time-machine/` directory.

The two conditions that make item 3 cheap are both still true: **Phase 16 has not
run, and the evidence has not moved.**

## 6. Verification state

- `verify-doc-paths.sh --all` — 0 MISSING, 0 ANCHOR BROKEN throughout.
  **Do not quote its `OK` count as a baseline** — Revision 130 stopped it
  drifting on parked notes, but it still moves legitimately whenever a tracked
  document gains a reference.
- `verify-runbook-structure.sh` — 29 FAIL / 5 WARN across 27 documents,
  unchanged all session.
- `verify-script-portability.sh` — 74 clean / 0 WARN / 0 FAIL.
- `bash -n` clean on every edited script.
- **All of it ran on Linux with Bash 5.x.** `shellcheck` was not available.
  `/bin/bash -n` against real macOS Bash 3.2 is still owed on Revisions 116–134.
