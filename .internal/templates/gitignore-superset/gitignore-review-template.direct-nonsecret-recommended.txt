# gitignore-review-template.direct-nonsecret-recommended.txt
#
# Optional, human-curated. Consumed by scan-gitignored-candidates.py via
# --nonsecret-template to build gitignored-selected-nonsecret-candidates.tsv —
# the "keep these non-secret ignored files" feed that the Phase 3A cert/Keychain
# plan reads. Without this file the scan falls back to a possibly-important +
# exclude seed for you to prune by hand.
#
# Same [ ]/[x] model as gitignore-review-template.txt: nothing is selected unless
# you check it. Every box below ships unchecked on purpose. Check the ones whose
# files you actually want carried forward as non-secret keeps.
#
# Secret-shaped patterns do NOT belong here — they are routed by
# secrets-patterns.txt into secrets-candidates/ for the encrypted DMG.

# ── Local, non-secret app config worth keeping ───────────────────────────
[ ] *.local.properties
[ ] application-local.yml
[ ] application-local.yaml
[ ] local.settings.json

# ── Editor and IDE settings you deliberately keep out of Git ─────────────
[ ] .idea/runConfigurations/
[ ] .idea/codeStyles/
[ ] .vscode/settings.json
[ ] .vscode/launch.json

# ── Local tooling config ─────────────────────────────────────────────────
[ ] .tool-versions
[ ] .nvmrc
[ ] .editorconfig.local

# ── Scratch and notes kept beside the code ───────────────────────────────
[ ] notes/
[ ] scratch/
[ ] TODO.local.md
