# Findings owned — `session-management-re-evaluation-20260906-110105`

Authoritative record of the findings bundles this session owns.
`docs/sessions/INDEX.md` carries the counts and points here rather than
restating the list. Each bundle's own index row names this session in return.

Statuses are defined in [`docs/legend.md`](../../legend.md).

All six were `unclaimed` — released by `restore-apps-outstanding-20260903-000000`
when it closed on 2026-09-06 — and were assigned together on the owner's word.
Assignment set each to `un-started`; the first reading, performed the same
sitting, moved each to `analyzing` with every finding in it `framing`.

| # | Bundle | Kind | Subject | Findings | Standing | Notes |
|---:|---|---|---|---:|---|---|
| 0036 | [`0036-verify-doc-paths-counts-gitignored-docs`](../../session-management-findings/0036-verify-doc-paths-counts-gitignored-docs/) | `session-management` | `verify-doc-paths.sh --all` counts `docs/`, so its OK baseline cannot hold | 1 | `answered` | **Resolved at Revision 225.** Option (iv): `docs/` is scanned, and `proposed` and `historical` are declarable — which is also `0046`'s remedy. 1714 OK / 0 MISSING / 0 broken anchors |

**1 bundle · 1 finding.**

**`0043` was transferred to [`entity-model-and-vocabulary-20260909-053548`](../entity-model-and-vocabulary-20260909-053548/) on 2026-09-09**, at the owner's
direction and for the same reason `0039` moved at Revision 248: this session
stands `handoff` with no successor, so it could not close the deciding on it —
and `findings[]` → `members[]` is a schema decision that closes deciding. It
gained F10 that day, contributed by a session that did not own it, which is what
*deliberately open* was for; **it stays open after the move.** Transfer changes
who may decide, not who may read or record, so a contribution from a non-owner is
as possible now as it was.

**`0036` did not move, and is the case for not sweeping.** It stands `answered`,
every finding `resolved`, and §10 permits a transfer only while a bundle is
`un-started`, `reopened` or `analyzing` — **the terminal standings have nothing to
move.** It is finished work correctly listed by the session that did it. With
`0043` gone this session holds one terminal bundle and no live reading, which is
the first moment its `handoff` has been true as `docs/legend.md` defines it.

**`0039` was transferred to [`typed-bundles-architecture-20260908-204724`](../typed-bundles-architecture-20260908-204724/) on 2026-09-09**, Revision 248, at the owner's ruling: this session
stands `handoff` with no successor, so it could not close the deciding on `0039`
F23. The reading is unchanged and this session's contributions to it stand.

*(This total read `3 bundles · 30 findings` against rows summing to 33 until
Revision 248. Revisions 239 and 240 added three findings to `0039` and updated
the row cell and `docs/sessions/INDEX.md` and not the sentence here — `0039` F21,
committed by the session that recorded it.)*

`0042-no-check-reads-the-rendered-page` sits in the same tree and is **not owned
by this session.** It is `unclaimed` and stays that way until the owner assigns
it; its F3 is a dependency decision that is the owner's to take.
