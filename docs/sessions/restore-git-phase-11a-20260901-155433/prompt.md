# Session prompt — Phase 11A, Restore Git

**No prompt survives for this session.** It was driven interactively against
`restore-git.md` rather than started from a written brief, and this file exists
because a session bundle requires one
(`.github/copilot-instructions.md` section 4d).

What the session was: the Phase 11A walk — `restore-git.md` — establishing SSH
keys, routing hosts and the `includeIf` commit identities that Phase 11B's clone
routing depends on. Its working record is `transcript-20260901-155433.txt` beside
this file, which is the only first-hand account of what was done.

Anyone reopening this work should read `restore-git.md` first, then the
transcript, and treat the transcript as a record of one sitting rather than as
instructions.
