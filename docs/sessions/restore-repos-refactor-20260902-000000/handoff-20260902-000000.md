# Session handoff — Restore Repositories Refactor, 2026-09-02

Executed [[docs/sessions/restore-repos-refactor-20260902-000000/restore-repos-phase-11b-plan|docs/sessions/restore-repos-refactor-20260902-000000/restore-repos-phase-11b-plan.md]]
from the prompt in [[docs/sessions/restore-repos-refactor-20260902-000000/prompt|docs/sessions/restore-repos-refactor-20260902-000000/prompt.md]].
Shipped as **Revision 131**, commit `9b6f67a`. The tree is clean; nothing is
pending from this session.

---

## What shipped

Phase 11B had been run three times. Every run emitted a `clone-commands.sh` with
**0** clone lines and 27 `no remote URL recorded` comments, and `rsync` commands
aimed at `/Users/dkittrell/Development/...` — a tree that does not exist on the
reimaged Mac and that `rsync -a` would have created, with decrypted DMG secrets
in it.

| # | Change | File |
|---|---|---|
| 1 | Squash tabs **and** stop `paste` cycling its delimiters | `.internal/git/capture-repo-audit.sh` |
| 2–4 | Route by remote host; test presence at the clone destination; aim both rsync scripts there **and guard on it** | `bin/restore-repos.sh` |
| 5 | Take the leading SHA only for the `cat-file -e` proof | `bin/restore-repos.sh` |
| 6 | Steps 10 and 11 — after/delta, then close out | `restore-repos.md` |
| 7 | Bundle Layout reconciled with what the script writes | `restore-repos.md` |
| 8 | "Neither file" → three files | `bin/restore-repos.sh` |
| 9 | One exit table, owned by the recorder; transport rows reworded | both |

**Two deviations from the plan, both approved in session.**

The plan's Change 1 was `tr '\t' ' '` alone. `paste -sd '; '` **cycles** its
delimiters, so every second join used a space — and `extract_remote_url`, which
splits on `;`, then saw two remote lines glued together. On
`carrier-services-storage` that resolved to `github.com/sadashiva108/dotfiles.git`.
Fixed at the source as `paste -sd ';'`.

Change 6 could not be `9a/9b/9c`. `verify-runbook-structure.sh` parses
`### Step N` with `$3 + 0`, so a lettered heading reads as its number and breaks
STEP-NUM. Became Steps 10 and 11, following `restore-access.md` and
`restore-runtime.md`.

## Decision A — executed as option (ii)

The pre-image machine is gone, so the damaged `repos.tsv` could not be
re-captured. `repo-audit-summary.txt` holds `git remote -v` verbatim — 60 URL
lines across 27 repositories — so it was re-derived into a **new indexed run**:

```text
repo-audit-reports/runs/pre-image-20260901-234636/
├── repos.tsv               # rebuilt remote column; every other cell as captured
├── RE-DERIVATION.md        # provenance: this is not a capture
├── derive-repos-tsv.py     # the derivation, kept with the data it produced
├── PINNED-OFFICIAL.txt
└── (every other file copied from the source run, cp -p)
```

Pinned via `artifact_run_set_official`, with the reason in `MANIFEST.md`.
`pre-image-20260816-035617` is **untouched** and stays the capture of record.

The derivation proves itself before writing: for each row it reconstructs the
*damaged* cell from the summary — same tabs, same cycling delimiter — and
requires a byte-exact match before emitting a corrected one. All 27 matched.

`repo-audit-summary.txt` was copied with `cp -p`, so it keeps its 2026-08-16
mtime and Phase 6B's freshness row still WARNs. A re-derivation must not make a
three-week-old capture look fresh.

**Measured** against a scratch artifact root (so no run, sign-off or pointer was
written to the volume): 25 clone lines, 2 `no remote` comments, 315 carry-forward
rows from the correct columns, zero `Development/` paths in any emitted script.

## What changed underneath, after Revision 131

The run-index session landed three revisions on top:

| Revision | Effect on Phase 11B |
|---|---|
| 132 | none — Time Machine evidence |
| 133 | none — `docs/architecture/` and `docs/ledgers/` became tracked directories |
| **134** | **the Phase 11B sign-off moved** to `reimaged-system/sign-offs/` |

Revision 134 rewrote three lines around `signoff_begin` in `bin/restore-repos.sh`
and did not touch `restore-repos.md`. It merged cleanly with this session's work.
A consequence worth knowing: the parked note saying `repo-audit-reports/sign-offs/`
does not exist is now correct *by design* rather than by accident.

Its commit message has `git add APPLY-MANIFEST.md bin/restore-repos.sh …`
appended to the subject line. Cosmetic; only a rewrite fixes it.

---

## Where to resume

**Walk `restore-repos.md` and confirm the fixes on the Mac.** Nothing upstream
needs patching — `record-restore-prereqs.sh` and `reimage-checklist.sh` both
resolve the audit through `artifact_run_official <root> pre-image`, so they pick
up the re-derived run with no change.

**Step 0a — re-run it.** The entry run on disk, `restore-repos-entry-20260825-033849`,
is **5 pass / 2 warn / 1 fail**. The FAIL is *Audit remote URLs are URLs*.
Running the check's own `awk` against both audits:

| Row | old run | re-derived |
|---|---|---|
| Audit remote URLs are URLs | 25 → FAIL | **0 → PASS** |
| Every repository has a remote | 2 → WARN | 2 → WARN |
| No repository spans both hosts | 2 → WARN | 2 → WARN |

Expect **6 pass / 2 warn / 0 fail**. `entry` is latest-wins, so it supersedes
cleanly. Both WARNs are real: `engagements` and `ingestion-related` have no
remote; `reference-vault` and `carrier-services-storage` span both hosts.

**Step 0b — do not re-run.** `restore-repos-before-20260825-034004` (0 present /
2 absent) is first-wins, clean and irreplaceable.

**Step 0c — nothing to do.** `GIT_PERSONAL_GITHUB_OWNER=sadashiva108` is already
in `reimage.env`.

**Step 1 — watch the report.** All three existing bundles have a **0-byte**
`restore-status.md` and no `MANIFEST.txt`, while `status.tsv` and all three
command files are complete. The same script produces both correctly on Linux, so
if it fails again it is Bash 3.2 in the report heredoc — run it under `bash -x`
and record the line. See
[[docs/runbook-findings/restore-repos/0017-post-image-restore-runs-truncated/findings|docs/runbook-findings/restore-repos/0017-post-image-restore-runs-truncated/findings.md]].

**Steps 10 and 11 will run.** `record-restore-state.sh` carries `restore-repos`
in `SUPPORTED_RUNBOOKS`; `record-restore-exit.sh` has a `restore-repos)` case.

Phase 11A is closed: two `restore-git-exit` runs on 2026-09-01 at 7 pass / 0 warn
/ 0 fail.

## Owed on the Mac

- `/bin/bash -n bin/restore-repos.sh` and `.internal/git/capture-repo-audit.sh`
  against real Bash 3.2 — owed on Revisions 116–134.
- Everything this session validated ran on **Linux with Bash 5.x**;
  `shellcheck` was unavailable.

## Parked, not fixed

Five notes from this session, all indexed in [[INDEX|the findings indexes]]. The
two that need a decision rather than a fix:

- [[docs/runbook-findings/restore-repos/0017-post-image-restore-runs-truncated/findings|Every post-image-restore run on disk stops before its report]] — reproduce on the Mac.
- [[docs/runbook-findings/restore-repos/0008-carrier-services-storage-foreign-remote/findings|`carrier-services-storage` carries a remote pointing at `dotfiles`]] — decide at Step 2 whether that remote comes back.

## Open design work

[[restore-repos-clone-plan|docs/architecture/restore-repos-clone-plan.md]] — a
declared clone plan replacing "clone the inventory verbatim into two roots".
Four fragments under `$REIMAGE_WORKSPACE_ROOT/repo-plan/`, seeded from
`.internal/templates/repo-plan/`, read at run time by `bin/restore-repos.sh`.

The finding that shaped it: `project-metadata/` mirrors the **IdeaProjects
directory tree**, not repo labels — 22 `.idea` directories, 18 matching a label,
4 belonging to no repository, keys nested as `apicoe/ingestion` and
`tools/copilot`. That mapping cannot be derived; it has to be declared. Hence a
rehydration **source registry** plus a per-repo **map**, rather than one flat
list.

Still open in that document: whether run bundles keep emitting runnable scripts;
how `record-restore-exit.sh` row 3 grades a deliberately relocated repository;
and Step 4's hardcoded toolkit path.

## Coordination

the findings indexes, `docs/sessions/INDEX.md` and `docs/INDEX.md` are new this
session — indexes rather than subdirectories, because `APPLY-MANIFEST.md` cites
three `docs/sessions/` files by full path and roughly fourteen `docs/*-findings/` paths
are cited from four other documents. Moving files would have broken citations in
the one file that has to stay accurate; see
[[docs/cross-cutting-findings/0009-dated-artifacts-cite-run-ids-a-rename-breaks/findings|docs/cross-cutting-findings/0009-dated-artifacts-cite-run-ids-a-rename-breaks/findings.md]],
which is a parked note about exactly that.

**The run-index session has not been told about the indexes yet.** They write
into `docs/*-findings/` regularly, and a row has to be added when a note is parked or
the index decays. The owner is handling that.
