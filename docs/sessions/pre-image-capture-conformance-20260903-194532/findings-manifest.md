# Findings owned — `pre-image-capture-conformance-20260903-194532`

Authoritative record of the findings bundles this session owns.
`docs/sessions/INDEX.md` carries the count and points here rather than
restating the list. Each bundle's own index row names this session in return.

Statuses are defined in [`docs/legend.md`](../../legend.md).

| # | Bundle | Kind | Subject | Findings | Status | What this session owes it |
|---:|---|---|---|---:|---|---|
| 0007 | `docs/runbook-findings/stage-loose-secrets/0007-content-scans-keeps-a-bespoke-index/` | runbook | `content-scans/` looks like a run category and is not one | 1 | `resolved` | Nothing. Assigned so this bundle answers for the whole set; read for the reasoning it preserves and for what the conversion turned up |
| 0013 | `docs/runbook-findings/capture-office-stability/0013-office-stability-checklists-are-evidence-bundles/` | runbook | `office-stability/checklists/` does not hold checklists | 1 | `unresolved` | A reading, and a report of where it stands. Its own disposition folds the fix into item 4, which `run-index-design-20260901-000000` still holds — this bundle owns the finding and not the work that resolves it |
| 0019 | `docs/runbook-findings/reimage-prep-checks/0019-reimage-checklist-repo-audit-manifest-header/` | runbook | Phase 6B records FAIL against a correct repository-audit manifest | 1 | `resolved` | Nothing. Closed by Revision 129. Confirm whether item 4's `reimage-prep-checks/` candidacy overlaps it |
| 0025 | `docs/runbook-findings/backup-repos/0025-staged-ignored-files-live-parent-root-bundles/` | runbook | `staged-ignored-files/live/` holds two bundles no lookup can reach | 1 | `unresolved` | A reading. Resolving it needs a look at what is in the two directories, which is an evidence read the owner has not authorised |

All four were **recorded** by session `01KcZvrKMgfenhrT9DvxW9Jk` and **owned** by
[`run-index-design-20260901-000000`](../run-index-design-20260901-000000/) until
the owner reassigned them here on 2026-09-03. That session keeps its other ten
findings and keeps item 4.

Two of the four are already `resolved` and are here so one bundle answers for the
whole set, not because they need work.
