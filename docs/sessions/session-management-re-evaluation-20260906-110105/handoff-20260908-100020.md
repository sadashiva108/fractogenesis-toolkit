# Handoff — session-management re-evaluation

**Written:** 2026-09-08 10:00 EST, **brought current** 2026-09-08 12:33 EST at Revision 228  
**Session:** `session-management-re-evaluation-20260906-110105` (`session_01FhFbEgG4wmrtJqUCcryNVQ`)  
**Composed against:** `55e753e` (Revisions 226 and 227)  
**Environment:** Linux VM on the owner's Mac — Bash 5.1.16, GNU coreutils, aarch64. **Not macOS.**

Handed off rather than closed: three bundles are still owned, and `0039` F8 is
`decided` with its toolkit write unmade.

## Disposal — four bundles released to `unclaimed`

Per §10a, and deliberately: these are the allocator's first real workload rather
than a successor's reading-order guess. All four are clean-slate supersession
successors with full provenance, nothing decided, and nothing owed.

| # | Findings | Released because |
|---:|---:|---|
| `0037` | 7 | Its `decisions.md` and `resolutions.md` were byte-identical clones of `0027`'s and were removed at Revision 224. Seven provenance edges. `0027` D2's `Outcome → rejected` is still owed and belongs with whoever reads it |
| `0038` | 6 | Clones of `0028`'s, removed. Six provenance edges. Nothing was ever decided in this bundle |
| `0040` | 4 | Clones of `0031`'s, removed. Four provenance edges |
| `0041` | 5 | Its pair were the **only** copies of `0032`'s work and were moved there, not deleted. Four provenance edges; F5 is new and has no incoming edge |

**Twenty-two findings, all `framing`.** None is blocked; none has a decision
waiting on it.

## Still owned: `0036`, `0039`, `0043` — 30 findings

`0036` is **`resolved`** and closed: one finding, option (iv) shipped at Revision
225. It stays listed because a session keeps what it resolved.

`0039` holds 20 findings — five resolved, two decided, thirteen framing — and is
the keystone. `0043` holds nine, two resolved, and is **held open on purpose** so
`allocation-and-inquiry-design-20260906-233205` can record to it.

## What a successor most needs to know

**1. A status moves on a material change to the record, never on a reading.**
`0039` D7. This session broke it before writing it: Revision 208 flattened
twenty-one `reopened` rows to `framing` believing it had only read them, and every
validator passed. `0039` F12.

**2. `reopened` and `superseded` are not alternatives, and the difference is an
address.** `reopened` needs the finding itself to have been `resolved`. `0038/F1`
never was; `0028/F1` was. The six bundles are supersession successors, so their
relationship to their predecessors is **provenance** — 31 edges, derived by
comparing statements, coverage and exclusivity passing on all six pairs.

**3. Status is data now.** All 54 `STATUS-`/`STATE-` files are gone. A bundle's
status derives from its findings in `metadata.json`; ownership derives from the
session manifests. The derivation was proved against all 54 tags before any was
deleted — **54 of 54 agreed.** `verify-findings-structure.sh` fails a bundle that
still carries a tag, so the door does not reopen.

**4. Resolving now has a procedure, and it did not.** §9b: carry out the decision,
write the `resolutions.md` row, move the status — in that order. Eighteen
decisions were accepted and seven carried out across Revisions 221–225 with
**nothing recorded** until the owner asked what had been resolved. `0039` F17.

**5. Nothing ran on Bash 3.2.** Six shell scripts and one Python helper have
self-location lines at depths never executed on the target. `/bin/bash -n` against
real Bash 3.2 is owed for these and for Revisions 116 onward.

## Owed, in pickup order

| # | Item | Why it is not done |
|---|---|---|
| 1 | `0039` F8 — install D6's hand-over step in §6 | Accepted 2026-09-07; the toolkit write was never made. The only decided finding whose work is outstanding |
| 2 | `0039` F17 — the check for a decided finding with no `resolutions.md` row | Held back deliberately: three checks shipped 2026-09-08 and **every one failed against a healthy tree on its first run**. The rule landed first |
| 3 | `state-as-data.md` §9 step 2 in full — regenerate every document from the JSON and diff | Run for the status field only. **The extraction's one outstanding proof** |
| 4 | The `headers` standing baseline — rows in `0005`, `0012`, `0013`, `0025`, `0030`, `0035` | One table cell each; owned by `run-index-design` and `pre-image-capture-conformance`, not this session's to edit |
| 5 | `0039` F13, F14, F18 | Recorded **unanswered on purpose** — the vocabulary rename, a README and quick-start, and whether `accepted` should say if the work was done |
| 6 | Four architecture records with run-on headers | `restore-docker-teardown-and-test.md`, `restore-repos-clone-plan.md`, `sign-off-consolidation.md`, `time-machine-run-index.md` — toolkit side, flagged not fixed |

## What was checked, and what was not

**Checked, on Linux:** all four session checks plus doc-paths, portability and
runbook-structure; `bash -n` on six scripts; `py_compile`; 54 files parsed as
JSON; a fence-and-table render check across every changed markdown file, which
found three table-splitting blank lines no committed validator sees.

**Not checked:** anything on macOS or Bash 3.2. The render check is a throwaway in
scratch and is not committed — **no checker in this repository reads a rendered
page**, which is `0042`.
